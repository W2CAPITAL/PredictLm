import test from 'node:test';
import assert from 'node:assert/strict';
import {codePromotionAllowed,detectResearchGaps,evaluateExperiment,harvestLearning,learningAudit,sourceTrust} from '../src/lib/continuous-learning/index';
test('typed continuous learning keeps unverified discoveries out of accepted evidence',()=>{const records=harvestLearning([],[{id:'x',observedAt:'2026-09-26T12:00:00Z',topic:'media-ai',kind:'github-discovery',title:'repo',summary:'timeline editor',source:'owner/repo',url:'https://github.com/owner/repo',confidence:0,allowlisted:false,status:'candidate'}]);assert.equal(records[0].status,'candidate');assert.ok(sourceTrust({kind:'github-allowlist',allowlisted:true,license:'MIT'})>=.88)});
test('gaps and audit remain explicit and production promotion stays blocked',()=>{const gaps=detectResearchGaps([],[{id:'media-ai',label:'Mídia',priority:5}],new Date('2026-09-26T12:00:00Z'));assert.equal(gaps[0].status,'open');const audit=learningAudit([],gaps,[]);assert.equal(audit.productionPromotion,'blocked');assert.equal(codePromotionAllowed({tests:true,build:true,reviewed:false,security:true}),false)});
test('experiment requires tests, build, quality and bounded latency',()=>{const ok=evaluateExperiment({baseline:{quality:80,latencyMs:1000,errors:1},candidate:{quality:84,latencyMs:1100,errors:0},testsPassed:true,buildPassed:true});assert.equal(ok.verified,true)});
