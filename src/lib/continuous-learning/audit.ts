import type {LearningAudit,LearningProposal,LearningRecord,ResearchGap} from './types';
export function learningAudit(records:LearningRecord[],gaps:ResearchGap[],proposals:LearningProposal[],at=new Date().toISOString()):LearningAudit{return{at,accepted:records.filter(x=>x.status==='accepted').length,candidates:records.filter(x=>x.status==='candidate').length,rejected:records.filter(x=>x.status==='rejected').length,gaps:gaps.filter(x=>x.status==='open').length,codeProposals:proposals.filter(x=>x.kind==='code').length,skillProposals:proposals.filter(x=>x.kind==='skill').length,productionPromotion:'blocked'}}
