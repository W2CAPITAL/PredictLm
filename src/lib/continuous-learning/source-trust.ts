import type {LearningRecord,LearningStatus} from './types';
export function sourceTrust(input:{kind?:string;official?:boolean;allowlisted?:boolean;license?:string;quarantined?:boolean}){if(input.quarantined)return 0;let score=.5;if(input.kind==='official-feed')score=.96;else if(input.kind==='github-allowlist')score=.9;else if(input.kind==='academic-preprint')score=.84;else if(input.kind==='rss')score=.72;else if(input.kind==='github-discovery')score=.56;if(input.official)score=Math.max(score,.93);if(input.allowlisted)score=Math.max(score,.88);if(/^(MIT|Apache-2\.0|BSD|ISC|MPL-2\.0)$/i.test(String(input.license||'')))score+=.02;return Math.max(0,Math.min(.99,Number(score.toFixed(2))))}
export function learningStatus(record:Pick<LearningRecord,'kind'|'confidence'|'allowlisted'|'quarantined'>,threshold=.74):LearningStatus{if(record.quarantined)return'rejected';if(record.kind==='github-discovery'&&!record.allowlisted)return'candidate';return Number(record.confidence||0)>=threshold?'accepted':'candidate'}
export function promotableEvidence(record:LearningRecord){return record.status==='accepted'&&!record.quarantined&&record.confidence>=.82}
