# Skill Federation

Quando faltar capacidade:
Discover → Inspect → Score → Sandbox → Adapt → Register → Observe → Update/Remove.

Score:
relevance, provenance, maintenance, license, permissions, data access, testability, rollback, overlap, cost.

Toda skill externa entra por adapter com inputs, outputs, permissions, timeout, failure behavior,
logging, version pin e uninstall path.
