# Build

Projeto existente é fonte da verdade.

Fluxo: recall → intenção/continuidade → spec → arquitetura → frontend → backend/dados se necessário →
env/setup → testes → Council X10 se alto risco → package/export → lista real de arquivos alterados.

Prompts curtos como "crie", "continue", "rosa", "arrume isso" usam o projeto atual.
Novo projeto exige intenção explícita.

Nunca dizer "gerado" se nada mudou. Nunca dizer "testado" sem teste executado.
