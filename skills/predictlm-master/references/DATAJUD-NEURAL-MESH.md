# DataJud + DJEN + Runtime Truth

Portable operational contract for PredictLM Master.

This document does not activate any capability by itself. A capability is considered active only when the current host/runtime proves it.

## Process/CNJ path

CNJ normalized
→ resolve tribunal
→ DataJud when available
→ DJEN when available
→ preserve partial results
→ official tribunal portal when needed/available
→ normalized timeline
→ cautious interpretation
→ answer.

Rules:
- empty response is not proof that a process or act does not exist;
- source failure is a limitation and must remain visible;
- never claim DataJud/DJEN was consulted unless an actual call/result exists in the current execution or the user supplied the result;
- separate official fact, user-provided evidence and inference;
- do not bypass CAPTCHA/WAF, third-party certificates or restricted records;
- filing, signature, payment, agreement and other legal-effect actions require human confirmation.

## DataJud

Use only when the host exposes the relevant route/network capability.

Useful outputs may include:
- movements;
- dates;
- movement names;
- closure/low signals when actually returned.

No result means only: no result from that consultation.

## DJEN

Use for publications/communications when the current host supports it.

Do not fabricate publication text. HTTP/WAF/availability failures must be reported as failures, not converted into invented content.

## Neural Local / WebGPU truth

Browser models are active only after a real load/self-test.

- Lite/Smart model names in documentation are configuration targets, not proof of a live runtime.
- navigator.gpu existing is not enough; a usable adapter/load must succeed.
- never claim GPU/WebGPU is active without runtime evidence.
- first visit must not silently download a large model.
- on weak machines prefer smaller runtime or no local model rather than fake capability.

## Provider Mesh truth

Remote providers are available only when the current server has valid configuration and the call succeeds.

- never claim a provider responded if it was not called successfully;
- provider health/cooldown is operational telemetry, not user memory;
- provider failure may change the route, never the requested subject;
- do not promote unrelated RAG/chunks into the answer;
- local runtimes may assist according to the current canonical PredictLM routing contract, but this reference must not override newer runtime policy.

## Runtime authority

When this portable reference conflicts with live code or a newer canonical skill:
1. current runtime/code wins;
2. PredictLM Master wins;
3. specialized canonical skill wins;
4. this portable reference is only supporting context.

The purpose of this file is operational honesty, not routing ownership.
